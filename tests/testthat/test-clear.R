# Instructions ----
#' This file follows the format generated by FIMS:::use_testthat_template().
#' Necessary tests include input and output (IO) correctness [IO
#' correctness], edge-case handling [Edge handling], and built-in errors and
#' warnings [Error handling]. See `?FIMS:::use_testthat_template` for more
#' information. Every test should have a @description tag, which can only span
#' one lines, that will be used in the bookdown report of the results from
#' {testthat}. This line can be more than 80 characters.


# clear ----
## Setup ----
load(test_path("fixtures", "integration_test_data.RData"))
data_frame <- FIMSFrame(data_big)

model_run <- create_default_configurations(data = data_frame) |>
  create_default_parameters(data = data_frame) |>
  initialize_fims(data = data_frame)

## IO correctness ----
test_that("`clear()` works with correct inputs", {
  #' @description No residual R or C++ memory structures remain after clear
  expect_no_warning(clear())
  expect_warning(clear_test())
})

## Error handling ----
test_that("`clear()` throws warning when dangling pointer or module exists", {
  #' @description clear_test() calls clear() with dangling module to trigger warning message
  expect_warning(clear_test())
})
