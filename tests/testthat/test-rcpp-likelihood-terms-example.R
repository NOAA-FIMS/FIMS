# Instructions ----
#' This file follows the format generated by FIMS:::use_testthat_template().
#' Necessary tests include input and output (IO) correctness [IO
#' correctness], edge-case handling [Edge handling], and built-in errors and
#' warnings [Error handling]. See `?FIMS:::use_testthat_template` for more
#' information. Every test should have a @description tag, which can only span
#' one lines, that will be used in the bookdown report of the results from
#' {testthat}. This line can be more than 80 characters.

# rcpp likelihood-term example ----
## Setup ----
example_path <- testthat::test_path(
  "..",
  "..",
  "inst",
  "examples",
  "likelihood_terms_rcpp_interface.R"
)

## IO correctness ----
test_that("direct Rcpp likelihood-term example runs", {
  #' @description Test that the direct Rcpp likelihood-term example runs and returns the expected term table shape.
  example_env <- new.env(parent = globalenv())

  expect_no_error(sys.source(example_path, envir = example_env))
  expect_s3_class(example_env$likelihood_terms, "data.frame")
  expect_equal(
    names(example_env$likelihood_terms),
    c("name", "type", "source_id")
  )
  expect_false(example_env$model$UsesLikelihoodTerms())

  example_text <- readLines(example_path, warn = FALSE)
  expect_false(any(grepl(
    "\\b(use_likelihood_terms|uses_likelihood_terms|get_likelihood_terms)\\b",
    example_text
  )))

  clear()
})

## Edge handling ----
# No edge cases to test.

## Error handling ----
# No built-in errors to test.
