# Set up your local environment to run the google tests locally

Intended for developers to set up their local environment prior to
running the integration tests.

## Usage

``` r
setup_gtest()
```

## Examples

``` r
if (FALSE) { # \dontrun{
setup_gtest()
} # }
```
