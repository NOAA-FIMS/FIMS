% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edm.R
\name{create_multivariate_edm_embedding}
\alias{create_multivariate_edm_embedding}
\title{Build a multivariate delay embedding from multiple time series}
\usage{
create_multivariate_edm_embedding(
  x,
  series_specs,
  E,
  tau,
  target_series = names(series_specs)[[1L]],
  drop_missing = TRUE,
  embedding_name = NULL
)
}
\arguments{
\item{x}{A \code{\link[=FIMSFrame]{FIMSFrame()}} object.}

\item{series_specs}{A named list of series specifications. Each element
must be a list with two character strings: \code{series_type} and
\code{series_name}, e.g.
\code{list(catch = list(series_type = "landings", series_name = "fleet1"), index = list(series_type = "index",    series_name = "survey1"))}.
All series must have the same number of observations after filtering.}

\item{E}{Positive integer. Number of time lags per series.}

\item{tau}{Positive integer. Time lag step.}

\item{target_series}{A single string matching one of the names in
\code{series_specs}. Identifies which series provides the target (one-step
ahead) values. Defaults to the first series.}

\item{drop_missing}{Logical. When \code{TRUE}, rows containing the FIMS
missing-value sentinel \code{-999} in any series are dropped. Default \code{TRUE}.}

\item{embedding_name}{A single string used as the storage key in
\code{edm_embeddings}. Defaults to
\code{paste0("mv_E", E, "_tau", tau, "_", paste(names(series_specs), collapse = "_"))}.}
}
\value{
A \code{\link[=FIMSFrame]{FIMSFrame()}} object identical to \code{x} with the new multivariate
embedding appended to the \code{edm_embeddings} slot.
}
\description{
Constructs a joint delay-embedding matrix from two or more time series
stored in a \code{\link[=FIMSFrame]{FIMSFrame()}} object and appends the result to the
\code{edm_embeddings} slot under \code{embedding_name}.

Each series contributes \code{E} lagged columns, so the total embedding width
is \code{n_series * E}. For \code{k} series \verb{x_1, ..., x_k}, row \code{t} of the matrix
is:
\deqn{[x_1(t),\, x_2(t),\, \ldots,\, x_k(t),\, x_1(t-\tau),\, \ldots,\,
       x_k(t-(E-1)\tau)]}

The target vector is taken from the series identified by \code{target_series}.
}
\details{
\strong{Fitting limitation}: The resulting embedding is stored as a pure R
numeric matrix. Full multivariate predictor support (passing this matrix
to the C++ Simplex / S-Map / GP functors) requires a
\code{construct_multivariate()} Rcpp method not yet exposed in the current
C++ layer. Calling \code{\link[=fit_edm]{fit_edm()}} on a multivariate embedding will raise an
informative error until that extension is merged. The matrix is immediately
useful for diagnostics, visualisation, and Convergent Cross Mapping (CCM)
via \code{model_edm_matrix_multivariate()}.
}
\examples{
\dontrun{
data("data_big")
ff <- FIMSFrame(data_big)
ff <- create_multivariate_edm_embedding(
  ff,
  series_specs = list(
    catch  = list(series_type = "landings", series_name = "fleet1"),
    index  = list(series_type = "index",    series_name = "survey1")
  ),
  E = 3L, tau = 1L,
  target_series = "catch"
)
mat <- model_edm_matrix_multivariate(ff, "mv_E3_tau1_catch_index")
}
}
\seealso{
\code{\link[=create_edm_embedding]{create_edm_embedding()}}, \code{\link[=model_edm_matrix_multivariate]{model_edm_matrix_multivariate()}},
\code{\link[=fit_edm]{fit_edm()}}
}
