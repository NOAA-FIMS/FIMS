% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/augment-fims.R
\name{augment.FIMSFit}
\alias{augment.FIMSFit}
\title{Augment a FIMSFit object for use with yardstick}
\usage{
\method{augment}{FIMSFit}(x, include_weights = TRUE, ...)
}
\arguments{
\item{x}{A \code{FIMSFit} object returned from \code{\link[=fit_fims]{fit_fims()}}.}

\item{include_weights}{Logical (default \code{TRUE}).  When \code{TRUE} and
\code{uncertainty} is available, a \code{.weight} column is added equal to
\code{1 / uncertainty^2} (inverse-variance weights).  Rows where
\code{uncertainty} is \code{NA} or zero receive \code{NA} weights, which yardstick
silently drops when calling weighted metrics.}

\item{...}{Unused; present for S3 method compatibility.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with at least the columns \code{.truth}, \code{.pred},
and optional \code{.weight}, plus grouping-metadata columns.
}
\description{
Returns a tidy tibble of observed-vs-expected pairs drawn from the output of
\code{\link[=get_estimates]{get_estimates()}}.  Only rows that have both an observed value and a
model-expected value are included (i.e. data-likelihood rows), so parameter
rows without data observations are automatically dropped.
}
\details{
The returned tibble follows the conventions expected by every
\code{\link[yardstick:metric_set]{yardstick::metric_set()}} metric function:\tabular{ll}{
   Column \tab Role \cr
   \code{.truth} \tab Observed data value (maps from \code{observed}) \cr
   \code{.pred} \tab Model-expected value (maps from \code{expected}) \cr
   \code{.weight} \tab Inverse-variance weight from \code{uncertainty} (optional) \cr
   \code{label} \tab Parameter / quantity label, e.g. \code{"landings_expected"} \cr
   \code{fleet} \tab Fleet identifier (integer) \cr
   \code{module_id} \tab Unique module identifier \cr
   \code{distribution} \tab Likelihood distribution used for this data stream \cr
   \code{year_i} \tab Year index (present when available in the estimates) \cr
   \code{age_i} \tab Age index  (present when available in the estimates) \cr
}
}
\examples{
\dontrun{
data("data_big")
data_4_model <- FIMSFrame(data_big)

fit <- setup_default_parameters(
  data = data_4_model
) |>
  initialize_fims(data = data_4_model) |>
  fit_fims(optimize = TRUE)

# Tidy tibble of observed vs. expected, ready for any yardstick metric
augment(fit)
}

}
\seealso{
\code{\link[=get_fit_metrics]{get_fit_metrics()}}, \code{\link[=get_estimates]{get_estimates()}}, \code{\link[yardstick:metrics]{yardstick::metrics()}}
}
