% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_data_distribution.R
\name{parse_data_distribution}
\alias{parse_data_distribution}
\title{Parse a distribution formula into structured components}
\usage{
parse_data_distribution(expr)
}
\arguments{
\item{expr}{One or more distribution specifications provided as a character
vector (for example, \code{"~ gaussian(mean = x, sd = 0.1)"}), an expression
(for example,
\code{expression(~ gaussian(mean = x, sd = 0.1))}), a language call produced
by \code{quote()}, or an R formula object.}
}
\value{
A column-oriented list. Every component has the same length as
\code{expr}, and every parameter supported by \link{fims_distributions} is
present. This includes:
\itemize{
\item \code{family}: a character vector containing the distribution family names.
\item \code{link}: a character vector containing the central parameter field names.
\item One list component for each supported distribution parameter.
}

Parameters that do not apply to a non-missing specification are represented
by \code{NA}. Missing specifications use \code{NA_character_} for the family and link,
\code{NA} for central parameters, and \code{1} for ancillary parameters. These neutral
ancillary values prevent missing observations from introducing \code{NA} into
vectorized likelihood calculations.
}
\description{
Parse one-sided formula-like distribution specifications and return their
distribution families and parameter expressions as an aligned list.
}
\details{
The parser accepts inputs that represent a formula containing a distribution
call on the right-hand side, such as \code{~ gaussian(mean = x, sd = 0.1)}.

Input handling and validation proceed in stages:
\itemize{
\item Character input is converted with \code{\link[base:parse]{base::str2lang()}}.
\item Elements of \code{expression()} input are extracted and parsed as language
objects.
\item A language call or formula object is treated as a scalar input.
\item Any other input type throws an error.
}

After conversion, the object must be a call whose head is \code{~}.
Only one-sided formulas (\code{~ dist(...)}) are supported.

The distribution call is decomposed into:
\itemize{
\item \code{family}: the function name used for the distribution, as a character
scalar (for example, \code{"gaussian"}, \code{"multinomial"}).
\item \code{parameters}: a list of unevaluated parameter expressions from the
distribution call. Named arguments preserve their names, while positional
arguments remain unnamed.
}

Parameter values are returned as language objects rather than evaluated
numeric vectors. This preserves references to symbols in the calling
environment (for example, \code{catch_expected}) so evaluation can occur later
in a context where those objects exist.
}
\examples{
FIMS:::parse_data_distribution(
  "~ dmultinom(prob = age_comp_expected, size = 300)"
)
FIMS:::parse_data_distribution(
  quote(~ dnorm(mean = 0, sd = 1))
)
FIMS:::parse_data_distribution(
  expression(~ dlnorm(meanlog = mu, sdlog = 0.1))
)
}
\keyword{internal}
