% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edm.R
\name{edm_compare}
\alias{edm_compare}
\title{Fit and compare multiple EDM methods on the same embedding}
\usage{
edm_compare(
  data,
  embedding_name = NULL,
  methods = c("simplex", "smap", "gp"),
  simplex_args = list(),
  smap_args = list(),
  gp_args = list()
)
}
\arguments{
\item{data}{A \code{\link[=FIMSFrame]{FIMSFrame()}} object containing at least one delay embedding
built with \code{\link[=create_edm_embedding]{create_edm_embedding()}}.}

\item{embedding_name}{A single string naming the embedding to use. If
\code{NULL} (default), the first embedding in \code{data} is used.}

\item{methods}{Character vector of methods to compare. Any subset of
\code{c("simplex", "smap", "gp")}. Default is all three.}

\item{simplex_args}{Named list of additional arguments forwarded to
\code{\link[=fit_edm]{fit_edm()}} when \code{method = "simplex"}. Common options:
\code{auto_select}, \code{n_neighbors}.}

\item{smap_args}{Named list of additional arguments forwarded to
\code{\link[=fit_edm]{fit_edm()}} when \code{method = "smap"}. Common options: \code{theta}.}

\item{gp_args}{Named list of additional arguments forwarded to
\code{\link[=fit_edm]{fit_edm()}} when \code{method = "gp"}. Common options: \code{gp_sigma2_init},
\code{gp_ve_init}, \code{gp_phi_init}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per method and columns:
\describe{
\item{method}{EDM algorithm name.}
\item{rho}{Pearson correlation between in-library predictions and
target values.}
\item{rmse}{Root-mean-square error.}
\item{mae}{Mean absolute error.}
\item{n}{Number of library points.}
\item{best}{Logical. \code{TRUE} for the method with the highest \code{rho}.}
\item{embedding_name}{The embedding used.}
}
Rows are sorted by \code{rho} descending.
}
\description{
Convenience wrapper that fits Simplex Projection, S-Map, and/or GP-EDM on
a shared delay embedding and returns a ranked comparison of in-library
prediction skill. Under the hood it calls \code{\link[=fit_edm]{fit_edm()}} and \code{\link[=edm_skill]{edm_skill()}}
for each requested method and binds the results into a single tibble,
sorted by descending Pearson correlation.
}
\examples{
\dontrun{
data("data_big")
ff <- FIMSFrame(data_big)
ff <- create_edm_embedding(ff, series_type = "landings",
                           series_name = "fleet1", E = 3L, tau = 1L)

edm_compare(ff)
edm_compare(ff, methods = c("simplex", "smap"),
            simplex_args = list(auto_select = TRUE),
            smap_args    = list(theta = 2.0))
}
}
\seealso{
\code{\link[=fit_edm]{fit_edm()}}, \code{\link[=edm_skill]{edm_skill()}}, \code{\link[=select_embedding_dimension]{select_embedding_dimension()}},
\code{\link[=select_n_neighbors]{select_n_neighbors()}}
}
