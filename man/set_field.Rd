% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fims_module_api.R
\name{set_variable_vector}
\alias{set_variable_vector}
\alias{set_numeric_vector}
\title{Set a module's values by field name}
\usage{
set_variable_vector(module, name, values, estimation_status)

set_numeric_vector(module, name, values)
}
\arguments{
\item{module}{A \link{fims_module}.}

\item{name}{The field name, as it appears in the module's documentation.}

\item{values}{The values to write.}

\item{estimation_status}{How the model should treat each element: one of
\code{"assumed_known"}, \code{"fixed_effects"}, \code{"random_effects"}, or
\code{"derived_quantity"}. Give one status to apply to every element, or one
per element. It is set here rather than fixed on the field, so the same
quantity can be estimated in one model and assumed known in another.}
}
\value{
\code{module}, invisibly, so calls can be chained.
}
\description{
\code{set_variable_vector()} writes a field that the model can estimate. Each
element carries an estimation status, and the field has an ID that a
distribution can refer to, so this is the setter for anything that might be
estimated or that a distribution applies to.

\code{set_numeric_vector()} writes a field that is plain numbers with no
estimation status: observations, uncertainties, ages, empirical weights.

Both replace the whole field, resizing it to match \code{values}.
}
\seealso{
\code{\link[=set_data]{set_data()}}, \code{\link[=get_variable_vector_id]{get_variable_vector_id()}}
}
