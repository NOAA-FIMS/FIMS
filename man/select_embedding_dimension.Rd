% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edm.R
\name{select_embedding_dimension}
\alias{select_embedding_dimension}
\title{Select the optimal embedding dimension for Simplex Projection}
\usage{
select_embedding_dimension(
  data,
  series_type,
  series_name,
  tau = 1L,
  E_min = 1L,
  E_max = 10L,
  drop_missing = TRUE
)
}
\arguments{
\item{data}{A \code{\link[=FIMSFrame]{FIMSFrame()}} object.}

\item{series_type}{A single string giving the \code{type} column value to filter
on, e.g. \code{"catch"} or \code{"index"}.}

\item{series_name}{A single string giving the \code{fleet} column value to filter
on, e.g. \code{"fleet1"} or \code{"survey1"}.}

\item{tau}{Positive integer. Time lag between successive coordinates.
Default is \code{1}.}

\item{E_min}{Positive integer. Minimum embedding dimension to evaluate.
Default is \code{1}.}

\item{E_max}{Positive integer. Maximum embedding dimension to evaluate.
Default is \code{10}.}

\item{drop_missing}{Logical. When \code{TRUE} (default), rows containing the FIMS
missing-value sentinel \code{-999} are dropped before constructing the
embedding.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per candidate \code{E} value and
columns:
\describe{
\item{E}{Candidate embedding dimension.}
\item{rho}{Pearson correlation between in-library predictions and
targets.}
\item{rmse}{Root-mean-square prediction error.}
\item{optimal}{Logical. \code{TRUE} for the \code{E} that maximises \code{rho}.}
}
}
\description{
Evaluates Simplex Projection prediction skill across a range of embedding
dimensions \code{E} using in-library (self-prediction) assessment. For each
candidate \code{E} value a delay embedding is constructed and Simplex Projection
is run with \code{n_neighbors = E + 1}. The Pearson correlation (\eqn{\rho})
and RMSE are computed between predictions and target values, and the \code{E}
that maximises \eqn{\rho} is flagged as optimal.

This function is typically the \strong{first step} in a univariate EDM workflow,
run before \code{\link[=create_edm_embedding]{create_edm_embedding()}} and \code{\link[=select_n_neighbors]{select_n_neighbors()}}.
}
\examples{
\dontrun{
data("data_big")
ff  <- FIMSFrame(data_big)
edim <- select_embedding_dimension(ff, series_type = "catch",
                                    series_name = "fleet1")
print(edim)
optimal_E <- edim$E[edim$optimal]
ff <- create_edm_embedding(ff, series_type = "catch",
                           series_name = "fleet1",
                           E = optimal_E, tau = 1L)
}
}
\seealso{
\code{\link[=select_n_neighbors]{select_n_neighbors()}}, \code{\link[=create_edm_embedding]{create_edm_embedding()}}, \code{\link[=fit_edm]{fit_edm()}}
}
