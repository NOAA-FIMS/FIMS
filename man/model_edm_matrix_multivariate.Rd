% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edm.R
\name{model_edm_matrix_multivariate}
\alias{model_edm_matrix_multivariate}
\title{Retrieve a multivariate delay-embedding matrix from a FIMSFrame}
\usage{
model_edm_matrix_multivariate(x, embedding_name = NULL, include_target = TRUE)
}
\arguments{
\item{x}{A \code{\link[=FIMSFrame]{FIMSFrame()}} object containing a multivariate delay embedding.}

\item{embedding_name}{A single string matching a multivariate embedding in
\code{x}. If \code{NULL}, the first multivariate embedding is used.}

\item{include_target}{Logical. When \code{TRUE} (default), a \code{target} column
containing the one-step-ahead target values is appended to the matrix.}
}
\value{
A numeric matrix with \code{n_rows} rows and \code{n_series * E} (or
\code{n_series * E + 1}) columns. Row and column names are set.
}
\description{
Returns the joint embedding matrix stored by
\code{\link[=create_multivariate_edm_embedding]{create_multivariate_edm_embedding()}} as a named numeric matrix, with
an additional \code{target} column appended for convenience.
}
\examples{
\dontrun{
mat <- model_edm_matrix_multivariate(ff, "mv_E3_tau1_catch_index")
head(mat)
}
}
\seealso{
\code{\link[=create_multivariate_edm_embedding]{create_multivariate_edm_embedding()}}
}
