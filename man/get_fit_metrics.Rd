% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/augment-fims.R
\name{get_fit_metrics}
\alias{get_fit_metrics}
\title{Compute yardstick metrics for a fitted FIMS model}
\usage{
get_fit_metrics(
  x,
  metrics = yardstick::metric_set(yardstick::rmse, yardstick::mae, yardstick::rsq),
  group_by = NULL,
  weighted = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{FIMSFit} object returned from \code{\link[=fit_fims]{fit_fims()}}.}

\item{metrics}{A \code{\link[yardstick:metric_set]{yardstick::metric_set()}}.  Defaults to
\code{yardstick::metric_set(yardstick::rmse, yardstick::mae, yardstick::rsq)}.
Any regression metric that accepts \code{truth} / \code{estimate} (and optionally
\code{case_weights}) can be included.}

\item{group_by}{A character vector of column names in the augmented tibble
to group by before computing metrics.  Common choices:
\itemize{
\item \code{"label"}        – one row-set per data-stream label
\item \code{"fleet"}        – one row-set per fleet
\item \code{"distribution"} – one row-set per likelihood distribution
\item \code{c("label", "fleet")} – per label × fleet combination
Defaults to \code{NULL} (no grouping).
}}

\item{weighted}{Logical (default \code{FALSE}).  When \code{TRUE}, inverse-variance
weights from \code{uncertainty} are passed to the metric functions via the
\code{case_weights} argument.  Only metrics that accept \code{case_weights} will
use them; others silently ignore the column.}

\item{...}{Additional arguments forwarded to \code{augment.FIMSFit()}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} in the standard yardstick result format:
columns \code{.metric}, \code{.estimator}, \code{.estimate}, plus any grouping columns.
}
\description{
Extracts observed-vs-expected pairs from a \code{FIMSFit} object via
\code{augment.FIMSFit()} and evaluates a \code{\link[yardstick:metric_set]{yardstick::metric_set()}} over them.
}
\details{
By default the metrics are computed over \strong{all} data streams combined.
Pass one or more column names to \code{group_by} to get per-stream breakdowns
(e.g., \code{group_by = "label"} gives one row per data-stream label such as
\code{"landings_expected"}, \code{"age_comp_expected"}, etc.).
}
\examples{
\dontrun{
data("data_big")
data_4_model <- FIMSFrame(data_big)

fit <- create_default_parameters(
  configurations = create_default_configurations(data = data_4_model),
  data = data_4_model
) |>
  initialize_fims(data = data_4_model) |>
  fit_fims(optimize = TRUE)

# Overall RMSE / MAE / R² across all data streams
get_fit_metrics(fit)

# Per-data-stream: one row-set per label
# (e.g. "landings_expected", "index_expected", "age_comp_expected")
get_fit_metrics(fit, group_by = "label")

# Per-fleet with a custom metric set
get_fit_metrics(
  fit,
  metrics  = yardstick::metric_set(yardstick::rmse, yardstick::mape),
  group_by = "fleet"
)

# Inverse-variance weighted metrics
get_fit_metrics(fit, weighted = TRUE)
}

}
\seealso{
\code{augment.FIMSFit()}, \code{\link[yardstick:metric_set]{yardstick::metric_set()}}
}
