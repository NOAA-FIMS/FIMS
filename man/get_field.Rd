% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fims_module_api.R
\name{get_variable_vector}
\alias{get_variable_vector}
\alias{get_numeric_vector}
\title{Read a module's values by field name}
\usage{
get_variable_vector(module, name)

get_numeric_vector(module, name)
}
\arguments{
\item{module}{A \link{fims_module}.}

\item{name}{The field name.}
}
\value{
For \code{get_variable_vector()}, a list with \code{values} and \code{estimation_status},
one status per element. For \code{get_numeric_vector()}, the values.
}
\description{
The counterparts to \code{\link[=set_variable_vector]{set_variable_vector()}} and \code{\link[=set_numeric_vector]{set_numeric_vector()}}: they
return what those functions wrote, so a value can be checked after setting
it.

\code{get_variable_vector()} returns the estimation statuses alongside the values,
because they are set together.

Both report the values given as \emph{input}. After a model is fitted the
estimates live in the model rather than in the module, so these do not change
when a model is fitted --- use \code{\link[=get_output]{get_output()}} or the fitted object for
estimates.
}
\seealso{
\code{\link[=set_variable_vector]{set_variable_vector()}}, \code{\link[=set_numeric_vector]{set_numeric_vector()}}
}
