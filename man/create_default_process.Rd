% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_default_parameters.R
\name{create_default_process}
\alias{create_default_process}
\title{Create default process for a FIMS model}
\usage{
create_default_process(
  input,
  module,
  par,
  process_distribution,
  estimated = FALSE,
  random = FALSE
)
}
\arguments{
\item{module}{Name of the process module.}

\item{par}{Name of the parameter that is random.}

\item{process_distribution}{Name of the process distribution.}

\item{random}{Boolean indicating whether of not the process is treated with the Laplace Approximation (TRUE) or Penalized Likelihood method (FALSE).}
}
\value{
A list containing the following two entries:
\describe{
\item{\code{parameters}:}{A list of parameter inputs for the FIMS
model.}
\item{\code{modules}:}{A list of modules with default or user-provided
settings.}
}
}
\description{
This function generates default process settings for a Fisheries
Integrated Modeling System (FIMS) model, including distribution name, estimation
and random settings. It applies default configurations when specific module settings
are not provided by the user.
}
\examples{
\dontrun{
data("data1")
fims_frame <- FIMSFrame(data1)
fleet1 <- survey1 <- list(
  selectivity = list(form = "LogisticSelectivity"),
  data_distribution = c(
    Index = "DlnormDistribution",
    AgeComp = "DmultinomDistribution"
  )
)
fleet2 <- list(
  selectivity = list(form = "DoubleLogisticSelectivity"),
  data_distribution = c(
    Index = "DlnormDistribution",
    AgeComp = "DmultinomDistribution",
    LengthComp = "DmultinomDistribution"
  )
)
default_parameters <- data |>
   create_default_parameters(
     fleets = fleets,
     recruitment = list(form = "BevertonHoltRecruitment"),
     growth = list(form = "EWAAgrowth"),
     maturity = list(form = "LogisticMaturity")
   ) |>
   create_default_process(
     module = "recruitment",
     par = "log_devs", expected = NULL,
     process_distribution = "DnormDistribution",
     estimated = TRUE,
     random = FALSE
   )
}
}
\seealso{
\itemize{
\item \code{\link[=create_default_parameters]{create_default_parameters()}}
\item \code{\link[=update_parameters]{update_parameters()}}
}
}
