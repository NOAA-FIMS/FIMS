% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edm.R
\name{edm_skill}
\alias{edm_skill}
\title{Compute in-library prediction skill for a fitted EDM model}
\usage{
edm_skill(fit, data)
}
\arguments{
\item{fit}{An \linkS4class{EDMFit} object returned by \code{\link[=fit_edm]{fit_edm()}}.}

\item{data}{The \code{\link[=FIMSFrame]{FIMSFrame()}} object that was passed to \code{\link[=fit_edm]{fit_edm()}}. Must
contain the named embedding referenced by \code{fit}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row and columns \code{rho}, \code{rmse},
\code{mae}, \code{n} (number of prediction-observation pairs used), \code{method},
and \code{embedding_name}.
}
\description{
Computes standard prediction skill metrics by comparing the in-library
predictions stored in an \linkS4class{EDMFit} object against the corresponding
observed target values retrieved from the originating \code{\link[=FIMSFrame]{FIMSFrame()}}.

Three metrics are returned:
\itemize{
\item \strong{rho} — Pearson correlation between predictions and observations.
\item \strong{rmse} — Root-mean-square error.
\item \strong{mae} — Mean absolute error.
}
}
\examples{
\dontrun{
data("data_big")
ff  <- FIMSFrame(data_big)
ff  <- create_edm_embedding(ff, series_type = "landings",
                             series_name = "fleet1", E = 3L, tau = 1L)
fit <- fit_edm(ff, method = "simplex")
edm_skill(fit, ff)
}
}
\seealso{
\code{\link[=fit_edm]{fit_edm()}}, \code{\link[=edm_forecast]{edm_forecast()}}, \code{\link[=select_n_neighbors]{select_n_neighbors()}}
}
