% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy-glance-fims.R
\name{tidy.FIMSFit}
\alias{tidy.FIMSFit}
\title{Tidy a FIMSFit object into a parameter-level tibble}
\usage{
\method{tidy}{FIMSFit}(
  x,
  parameters = c("fixed_effects", "random_effects"),
  conf.int = FALSE,
  conf.level = 0.95,
  ...
)
}
\arguments{
\item{x}{A \code{FIMSFit} object returned from \code{\link[=fit_fims]{fit_fims()}}.}

\item{parameters}{Character vector controlling which \code{estimation_type}
values to include.  Defaults to \code{c("fixed_effects", "random_effects")}.
Pass \code{"derived_quantity"} to include derived quantities such as spawning
biomass and expected data values, or pass all three to get every row.}

\item{conf.int}{Logical (default \code{FALSE}).  When \code{TRUE}, \code{conf.low} and
\code{conf.high} columns are added using a normal approximation:
\verb{estimate +/- qnorm((1 + conf.level) / 2) * std.error}.}

\item{conf.level}{Numeric (default \code{0.95}).  The confidence level used when
\code{conf.int = TRUE}.}

\item{...}{Unused; present for S3 method compatibility.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with columns:
\describe{
\item{\code{term}}{Parameter label (from \code{label} in \code{\link[=get_estimates]{get_estimates()}}).}
\item{\code{estimate}}{Point estimate at the MLE.}
\item{\code{std.error}}{Standard error from \code{\link[TMB:sdreport]{TMB::sdreport()}}.}
\item{\code{statistic}}{Wald z-statistic (\code{estimate / std.error}).}
\item{\code{p.value}}{Two-sided p-value for the z-test.}
\item{\code{conf.low}, \code{conf.high}}{Confidence bounds (only when
\code{conf.int = TRUE}).}
\item{\code{module_name}}{Name of the FIMS module (e.g. \code{"Selectivity"}).}
\item{\code{module_id}}{Integer module identifier.}
\item{\code{estimation_type}}{One of \code{"fixed_effects"}, \code{"random_effects"},
or \code{"derived_quantity"}.}
\item{\code{gradient}}{Gradient of the log-likelihood at the MLE. Values
close to zero indicate a well-converged parameter.}
}
}
\description{
Returns one row per estimated parameter following the
\code{\link[generics:tidy]{generics::tidy()}} convention.  Standard columns (\code{term}, \code{estimate},
\code{std.error}, \code{statistic}, \code{p.value}) are always present; FIMS-specific
columns (\code{module_name}, \code{module_id}, \code{estimation_type}, \code{gradient}) are
appended so the full context is available for filtering and plotting.
}
\section{Parameter types}{

FIMS distinguishes three \code{estimation_type} values:
\describe{
\item{\code{"fixed_effects"}}{Directly optimized parameters (selectivity,
log_Fmort, log_q, etc.).}
\item{\code{"random_effects"}}{Integrated-out random effects (log_devs, etc.).}
\item{\code{"derived_quantity"}}{Model outputs that are not parameters
(spawning biomass, expected catches, etc.). Uncertainty here comes from
the delta method via \code{\link[TMB:sdreport]{TMB::sdreport()}}.}
}
Pass any subset of these strings to \code{parameters} to control which rows are
returned.
}

\section{Inference}{

\code{statistic} and \code{p.value} are computed as a two-sided Wald z-test:
\code{z = estimate / std.error},
\verb{p = 2 * pnorm(-|z|)}.
These are asymptotically valid for fixed effects under regularity
conditions; treat them as approximate for random effects and derived
quantities.
}

\examples{
\dontrun{
data("data_big")
data_4_model <- FIMSFrame(data_big)

fit <- create_default_parameters(
  configurations = create_default_configurations(data = data_4_model),
  data = data_4_model
) |>
  initialize_fims(data = data_4_model) |>
  fit_fims(optimize = TRUE)

# Fixed and random effects (default)
tidy(fit)

# Fixed effects only, with 95\% confidence intervals
tidy(fit, parameters = "fixed_effects", conf.int = TRUE)

# All rows including derived quantities
tidy(fit, parameters = c("fixed_effects", "random_effects", "derived_quantity"))
}

}
\seealso{
\code{\link[=glance.FIMSFit]{glance.FIMSFit()}}, \code{\link[=get_estimates]{get_estimates()}}, \code{\link[=fit_fims]{fit_fims()}}
}
