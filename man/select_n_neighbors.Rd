% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edm.R
\name{select_n_neighbors}
\alias{select_n_neighbors}
\title{Select the optimal number of nearest neighbours for Simplex Projection}
\usage{
select_n_neighbors(data, embedding_name = NULL, k_min = NULL, k_max = NULL)
}
\arguments{
\item{data}{A \code{\link[=FIMSFrame]{FIMSFrame()}} object with at least one delay embedding
created by \code{\link[=create_edm_embedding]{create_edm_embedding()}}.}

\item{embedding_name}{A single string matching a named embedding in \code{data}.
If \code{NULL}, the first available embedding is used.}

\item{k_min}{Positive integer. Minimum number of neighbours to evaluate.
Defaults to \code{E + 1}.}

\item{k_max}{Positive integer. Maximum number of neighbours to evaluate.
Defaults to \code{min(floor(n_library / 2), E + 10)}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per candidate value and columns:
\describe{
\item{n_neighbors}{Candidate neighbour count.}
\item{rho}{Pearson correlation between predictions and targets.}
\item{rmse}{Root-mean-square prediction error.}
\item{optimal}{Logical. \code{TRUE} for the row that maximises \code{rho}.}
}
}
\description{
Evaluates Simplex Projection prediction skill across a range of
\code{n_neighbors} values using in-library (self-prediction) assessment.
For each candidate value the function computes the Pearson correlation
(\eqn{\rho}) and root-mean-square error (RMSE) between the in-library
predictions and the embedding target values, then flags the value that
maximises \eqn{\rho}.
}
\examples{
\dontrun{
data("data_big")
ff <- FIMSFrame(data_big)
ff <- create_edm_embedding(ff, series_type = "catch", series_name = "fleet1",
                           E = 3L, tau = 1L)
skill <- select_n_neighbors(ff)
print(skill)
}
}
\seealso{
\code{\link[=fit_edm]{fit_edm()}}, \code{\link[=create_edm_embedding]{create_edm_embedding()}}
}
