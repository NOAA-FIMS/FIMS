// Instructions ----
// This file follows the format generated by FIMS:::use_google_benchmark_template().
// Use this template to add a performance benchmark for a specific workload.
// Benchmarks typically reuse the same fixture as the corresponding unit test
// so that setup stays aligned. See `?FIMS:::use_google_benchmark_template` for
// more information. Run the benchmark executable (e.g. build then
// ./build/tests/google_benchmark/benchmark_<name>) to measure; do not run
// benchmarks as part of the regular test suite.
// Google Benchmark user guide:
// https://google.github.io/benchmark/user_guide.html

#include "benchmark/benchmark.h"
#include "gtest/gtest.h"

// TODO: Include the header for the test fixture you want to reuse, or remove
// if defining a dedicated benchmark-only fixture.
// For example, to benchmark the same workload as test_population_CatchNumbersAtAge.cpp:
// #include "../gtest/test_population_test_fixture.hpp"
#include "path/to/test_fixture_or_header.hpp"

namespace {

// TODO: Replace with the actual fixture type from the include above, or define
// a struct that sets up the state needed for the code under benchmark.
// If reusing a gtest fixture, inherit from it and provide Init() and TestBody()
// (see tests/google_benchmark/benchmark_population_CatchNumbersAtAge.cpp).
struct BenchFixture {
  void Init() {
    // TODO: If reusing a gtest fixture, call SetUp() here.
    // SetUp();
  }

  void TestBody() {}

  // TODO: Add a method that runs only the production code you want to time,
  // so the benchmark loop measures just that (setup runs once per benchmark).
  // double RunBenchmarkedCode() { ... }
};

// Benchmark: {{{ benchmark_name }}}
static void BM_{{{ benchmark_name }}}(benchmark::State& state) {
  BenchFixture fx;
  fx.Init();

  for (auto _ : state) {
    // TODO: Call the code under benchmark and prevent the compiler from
    // optimizing it away. Example:
    // double result = fx.RunBenchmarkedCode();
    // benchmark::DoNotOptimize(result);
    benchmark::DoNotOptimize(fx);
  }
}
BENCHMARK(BM_{{{ benchmark_name }}});

}  // namespace
