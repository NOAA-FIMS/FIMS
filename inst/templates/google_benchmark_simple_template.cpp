// Instructions ----
// This file follows the format generated by FIMS:::use_google_benchmark_template().
// Use this simple template when you can benchmark production code directly
// without a gtest fixture.
//
// See `?FIMS:::use_google_benchmark_template` for more information. Run the
// benchmark executable (e.g. build then
// ./build/tests/google_benchmark/benchmark_<name>) to measure; do not run
// benchmarks as part of the regular test suite.
// Google Benchmark user guide:
// https://google.github.io/benchmark/user_guide.html

#include "benchmark/benchmark.h"

// TODO: Include the production header that defines the code under benchmark.
// Example:
// #include "../../inst/include/common/math.hpp"
#include "path/to/production_header.hpp"

namespace {

// Benchmark for {{{ class_name }}}::{{{ function_name }}}
static void BM_{{{ class_name }}}_{{{ function_name }}}(benchmark::State& state) {
  // TODO: Create representative inputs outside the loop.
  // auto x = ...;

  for (auto _ : state) {
    // TODO: Call the code under benchmark and prevent optimization.
    // auto result = {{{ function_name }}}(x);
    // benchmark::DoNotOptimize(result);
    benchmark::DoNotOptimize(state.iterations());
  }
}
BENCHMARK(BM_{{{ class_name }}}_{{{ function_name }}});

}  // namespace
